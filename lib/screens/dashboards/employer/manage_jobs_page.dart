import 'package:flutter/material.dart';

class ManageJobsPage extends StatelessWidget {
  const ManageJobsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Manage Job Posts", style: TextStyle(fontSize: 20)),
    );
  }
}
